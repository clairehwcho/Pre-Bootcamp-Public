function logout (element) {
    element.innerText = "Logout"
}

function hide(element) {
    element.remove();
}

function over(element) {
    element.style.backgroundColor = "Indigo";
}

function out(element) {
    element.style.backgroundColor = "#2B78E4";
}